---
permalink: /search-index.json
---

[
  {% for article in collections.search %}
    {
      "id": "{{ article.url }}",
      "title": "{{ article.data.title }}"
    } {% if not loop.last %},{% endif %}
  {% endfor %}
]
